# Recovered from lab-hv01 — 2026-08-31

Source files that lived ONLY on the lab Hyper-V host `lab-hv01` and were in no git
repository on any forge. Recovered 2026-08-31 into the private preservation repo
`aollivierre/lab-hv01-unversioned-code`, and triaged into this repo on 2026-09-03.

* Paths mirror their original location on the host, so provenance stays checkable.
* Nothing here has been reorganised, rewritten or built — it is a faithful record,
  with one exception: because this repo is **public**, two account names were
  pseudonymised in both paths and file contents (`JSMITH` -> `USER`,
  `Admin-Abdullah` -> `ADMIN`). Nothing else was altered.
* Scanned before publication: no private IPs, internal domains, email addresses,
  credentials or tokens. GUIDs present are public Microsoft identifiers.
* This is an **archive branch**. It is not merged into the default branch and is not
  expected to build.
